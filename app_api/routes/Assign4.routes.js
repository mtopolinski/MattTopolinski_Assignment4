/*
module.exports = function(app) {

    var Assign4 = require('./routes/list.js');



    // Create a new Note
    app.post('/Assign4s', Assign4s.create);

    // Retrieve all Notes
    app.get('/Assign4s', Assign4s.findAll);

    // Retrieve a single Note with noteId
    app.get('/Assign4s/:Assign4Id', Assign4s.findOne);

    // Update a Note with noteId
    app.put('/Assign4s/:Assign4Id', Assign4s.update);

    // Delete a Note with noteId
    app.delete('/Assign4s/:Assign4Id', Assign4s.delete);
}

*/

module.exports = function(app) {

}